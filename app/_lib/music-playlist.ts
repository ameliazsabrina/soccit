// Add new background tracks to public/sounds, then list their public paths here.
// Playback starts at a random track and avoids immediate repeats.
export const MUSIC_TRACKS: readonly [string, ...string[]] = [
  "/sounds/music.mp3",
] as const;
